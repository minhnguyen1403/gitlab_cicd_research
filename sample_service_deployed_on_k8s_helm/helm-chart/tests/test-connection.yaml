apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kf-release-users.fullname" . }}-test-connection"
  labels:
    {{- include "kf-release-users.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kf-release-users.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never